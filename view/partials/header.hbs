<header class="header">
    <div class="container">
        <div class="header-content">
            <!-- Logo -->
            <div class="logo">
                <span class="logo-icon">🌱</span>
                <span class="logo-text" data-key="siteName">{{t 'siteName'}}</span>
            </div>

            <!-- Header Actions -->
            <div class="header-actions">
                <!-- Language Dropdown -->
                <div class="language-dropdown">
                    <button class="language-toggle" id="languageToggle">
                        <span class="current-flag">{{#eq lang 'en'}}🇺🇸{{else}}🇮🇳{{/eq}}</span>
                        <span class="current-lang">{{#eq lang 'en'}}English{{else}}हिंदी{{/eq}}</span>
                        <span class="dropdown-arrow">▼</span>
                    </button>
                    <div class="language-dropdown-menu" id="languageDropdown">
                        <button class="language-option" data-lang="en">
                            <span class="flag">🇺🇸</span>
                            <span>English</span>
                        </button>
                        <button class="language-option" data-lang="hi">
                            <span class="flag">🇮🇳</span>
                            <span>हिंदी</span>
                        </button>
                    </div>
                </div>

                <!-- Login Button -->
                <button class="login-btn" data-key="login" onclick="handleLogin()">
                    {{t 'login'}}
                </button>
            </div>
        </div>
    </div>
</header>